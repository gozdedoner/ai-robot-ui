import React from "react";
import "../index.css";

const Header: React.FC = () => {
  return (
    <header className="app-header">
      <h1>
        Chat Rabot <span className="neon-text">AI</span>
      </h1>
    </header>
  );
};

export default Header;
